# API URL — used at build time by Vite
# In Docker: set via ARG in Dockerfile or docker-compose environment
# In dev: proxied automatically via vite.config.js (no need to set this)
VITE_API_URL=http://localhost:3001
